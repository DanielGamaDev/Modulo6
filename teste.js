let altura = prompt("Qual a altura?");

let largura = prompt("Qual a largura?");

function calculaArea(altura, largura){
    a = altura;
    l = largura;
    areatotal = (a*l);
    return areatotal;
} 

alert ("A area total é igual a " + areatotal)
